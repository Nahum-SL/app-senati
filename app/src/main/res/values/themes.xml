<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Iniciar_sesion" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="buttonInfo" parent="Widget.Material3.Button">
        <item name="cornerRadius">0dp</item>
        <item name="strokeWidth">2dp</item>
        <item name="strokeColor">@color/black</item>
        <item name="textAllCaps">true</item>
    </style>

    <style name="Theme.Iniciar_sesion.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/cby</item>
        <item name="postSplashScreenTheme">@style/Theme.Iniciar_sesion</item>
    </style>

    <style name="Theme.Iniciar_sesion" parent="Base.Theme.Iniciar_sesion" />
</resources>